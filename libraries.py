#this program will not work as we dont have libraries
import matplotlib.pyplot as plt #to make graph
import numpy as np #to make array
x=[[1,2,3],[2,4,5],[3,4,6]]
a=np.array(x)
  # %matplotlib inline
new_obj.resample('M').sum().plot(kind="bar")
plt.show()
x=np.linspace(0,10,100)
y=x**2
plt.plot(x,y)

#for more detail reffer this
# https://docs.python.org/3/library/index.html

